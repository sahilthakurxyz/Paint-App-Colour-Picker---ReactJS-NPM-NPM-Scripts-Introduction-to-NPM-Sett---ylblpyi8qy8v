import React from "react";

function Selection(props) {
  return (
    <div className="fix-box" style={{ backgroundColor: props.applyColor }}>
      <h2 className="subheading" onClick={() => {}}>
        Selection
      </h2>
    </div>
  );
}

export default Selection;
